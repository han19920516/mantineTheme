import { MantineThemeOverride, MantineProvider } from '@mantine/core';

const overrideMantineTheme: MantineThemeOverride = {
  colorScheme: 'light',
  defaultRadius: 0,
};

export default overrideMantineTheme;